{
  "name": "syncbridge",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "desktop": "npm --workspace apps/desktop run dev",
    "api": "npm --workspace apps/api run dev",
    "networking": "npm --workspace apps/networking run dev",
    "build:networking": "npm --workspace apps/networking run build",
    "dev": "npm run build:networking && concurrently -k \"npm --workspace apps/networking run build -- -w\" \"npm --workspace apps/api run dev\" \"npm --workspace apps/desktop run dev\"",
    "desktop:electron": "npm run build:networking && npm --workspace apps/desktop run build && npm --workspace apps/desktop run electron",
    "check": "npm --workspace apps/networking run check && npm --workspace apps/api run check && npm --workspace apps/desktop run check && npm --workspace protocol run check"
  },
  "devDependencies": {
    "@types/electron": "^1.4.38",
    "electron": "^44.2.0",
    "typescript": "^7.0.2",
    "concurrently": "^8.2.2"
  }
}
